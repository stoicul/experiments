services:
  benchmark:
    image: ruby:3.3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["ruby"]
    command: ["scripts/run_all.rb"]
    environment:
      - NUM_ENTRIES=${NUM_ENTRIES:-10000000}
    deploy:
      resources:
        limits:
          memory: 15G
